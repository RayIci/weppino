version: "3"

includes:
  shared-kernel:
    taskfile: ./libs/shared-kernel/Taskfile.yml
    dir: ./libs/shared-kernel
  order-service:
    taskfile: ./services/order-service/Taskfile.yml
    dir: ./services/order-service
  store-website:
    taskfile: ./apps/store-website/Taskfile.yml
    dir: ./apps/store-website

tasks:
  setup:
    desc: Install developer tools and wire up git hooks (run once after cloning)
    cmds:
      - bash scripts/setup.sh

  format:
    desc: Format all projects
    deps: [shared-kernel:format, order-service:format, store-website:format]

  lint:
    desc: Lint all projects
    deps: [shared-kernel:lint, order-service:lint, store-website:lint]

  test:
    desc: Test all projects
    deps: [shared-kernel:test, order-service:test, store-website:test]
