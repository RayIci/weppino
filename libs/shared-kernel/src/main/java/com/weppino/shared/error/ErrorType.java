package com.weppino.shared.error;

/** Enum representing different types of errors that can occur in the system. */
public enum ErrorType {
  VALIDATION,
  NOT_FOUND,
  UNAUTHORIZED,
  CONFLICT,
  INTERNAL
}
